{
  "project": {
    "name": "dev-experiment",
    "description": "Local development run — small dataset, fast iterations"
  },
  "executor": {
    "kind": "local",
    "concurrency": {
      "mode": "sequential"
    }
  },
  "artifacts": {
    "root": "./artifacts/dev"
  },
  "seed": 0,
  "dataset_hash": "epd-v1.0-sample",
  "experiment": {
    "model_id": "bert-base-uncased",
    "data_version": "epd-v1.0-sample",
    "epochs": 3,
    "batch_size": 16,
    "max_per_species": 500
  }
}
